import HeroBox from "./HeroBox";
const Hero = () => {
    return(
        <section className="hero grid">
            <div className="hero__content">
                <HeroBox />
            </div>
        </section>
    );
};
export default Hero;